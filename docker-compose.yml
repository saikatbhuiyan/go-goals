services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5434:5432"
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
